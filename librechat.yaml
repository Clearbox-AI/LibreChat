# For more information, see the Configuration Guide:
# https://www.librechat.ai/docs/configuration/librechat_yaml

# Configuration version (required)
version: 1.3.11
# This setting caches the config file for faster loading across app lifecycle
cache: false # slower, but could cause less problems to end clients when we update the site

mcpServers:
  # File system access
  replica:
    type: sse
    url: http://172.18.0.1:8080/sse
    serverInstructions: true

# Exempt the Docker-host private IP from MCP SSRF protection so LibreChat can
# reach the local "replica" server. Scoped to a single host:port pair.
mcpSettings:
  allowedAddresses:
    - "172.18.0.1:8080"

endpoints:
  agents:
    recursionLimit: 50
    maxRecursionLimit: 100
    disableBuilder: false
    # (optional) File citation configuration for file_search capability
    maxCitations: 30          # Maximum total citations in responses (1-50)
    maxCitationsPerFile: 7    # Maximum citations from each file (1-10)
    minRelevanceScore: 0.45   # Minimum relevance score threshold (0.0-1.0)
    capabilities:
      - "execute_code"
      - "file_search"
      - "actions"
      - "tools"
#      - "artifacts" # disable artifacts
      - "ocr"
      - "chain"
      - "web_search"
      - "context"
      - "artifacts"
      - "skills"


registration:
  allowedDomains:
    - "clearbox.ai"
    - "omc.com"


interface:
#  mcpServers:
#    placeholder: "MCP Servers"
#  privacyPolicy:
#    externalUrl: "https://example.com/privacy"
#    openNewTab: true
#  termsOfService:
#    externalUrl: "https://example.com/terms"
#    openNewTab: true
#    modalAcceptance: true
#    modalTitle: "Terms of Service"
#    modalContent: |
#      # Terms of Service
#      ## Introduction
#      Welcome to LibreChat!
  modelSelect: true
  parameters: false
  presets: false
  prompts: false
  bookmarks: false
  multiConvo: false
  agents: true
  artifacts: true
#  customWelcome: "Hey {{user.name}}! Welcome to LibreChat"
  runCode: false
  webSearch: true
  memories: false
  skills:
    defaultActiveOnShare: true
  fileSearch: false
  fileCitations: false
  defaultEnabledMcpTools:
    - "replica"


# disable file upload
fileConfig:
  endpoints:
    anthropic:
      disabled: true

